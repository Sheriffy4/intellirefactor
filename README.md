# IntelliRefactor

Интеллектуальная система анализа и автоматического рефакторинга Python проектов. Анализирует код, выявляет проблемы, предлагает улучшения и безопасно применяет изменения.

## 📦 Установка

### Требования
- Python 3.8+
- Windows/Linux/macOS

### Быстрая установка
```bash
# Клонирование репозитория
cd c:\Intel\recon

# Установка зависимостей
pip install -r requirements.txt

# Установка IntelliRefactor в режиме разработки
pip install -e .
```

### Верификация установки
```bash
# Проверка версии
python -m intellirefactor --version

# Тестовый запуск
python -m intellirefactor analyze --help
```

## 🚀 Примеры CLI

### Базовый анализ проекта
```bash
# Анализ всего проекта
python -m intellirefactor analyze .

# Анализ с генерацией диаграмм
python -m intellirefactor analyze . --visualize

# Анализ с сохранением результатов
python -m intellirefactor analyze . --output analysis_results.json
```

### Поиск возможностей для рефакторинга
```bash
# Показать топ-10 возможностей
python -m intellirefactor opportunities .

# Показать все возможности (без лимита)
python -m intellirefactor opportunities . --limit 0

# Сохранить список возможностей
python -m intellirefactor opportunities . --output opportunities.json
```

### Безопасный рефакторинг
```bash
# Предварительный просмотр изменений (dry-run)
python -m intellirefactor refactor . --dry-run

# Рефакторинг с консервативным уровнем безопасности
python -m intellirefactor refactor . --safety-level conservative

# Рефакторинг с ограничением операций
python -m intellirefactor refactor . --max-operations 5
```

### Применение конкретных улучшений
```bash
# Применить конкретную возможность
python -m intellirefactor apply opportunity.json

# Применить с предварительным просмотром
python -m intellirefactor apply opportunity.json --dry-run
```

### Работа с базой знаний
```bash
# Запрос к базе знаний
python -m intellirefactor knowledge query "dependency injection"

# Просмотр всех паттернов
python -m intellirefactor knowledge list-patterns
```

## ⚙️ Как работает пайплайн

### 1. Индексация и анализ
```
Исходный код → AST парсинг → Семантический анализ → Индексация символов
     ↓
Метрики кода ← Цикломатическая сложность ← Размер файлов ← Связанность
```

### 2. Выявление возможностей
```
Индекс кода → Паттерн-матчинг → Метрический анализ → Ранжирование
     ↓
Список возможностей ← Приоритеты ← Сложность реализации ← Потенциал улучшения
```

### 3. Генерация и применение изменений
```
Выбранная возможность → Шаблонное преобразование → Валидация → Применение
     ↓
Резервные копии ← Тестирование ← Проверка обратной совместимости ← Отчет об изменениях
```

### 4. Валидация и отчетность
```
Измененный код → Автотесты → Метрическая проверка → Генерация отчета
     ↓
Откат при ошибке ← Сравнение метрик ← Документация изменений ← Уведомления
```

## ⚠️ Ограничения

### Технические ограничения
- Поддерживает только Python 3.8+
- Ограниченная поддержка динамического кода
- Не обрабатывает C-расширения
- Может не распознавать сложные метапрограммные паттерны

### Безопасность
- Не рекомендуется для production без тестирования
- Всегда используйте `--dry-run` перед реальными изменениями
- Рекомендуется делать резервные копии проекта
- Некоторые трансформации могут нарушить обратную совместимость

### Производительность
- Большой проекты (>100k строк) требуют значительного времени анализа
- Интенсивное использование RAM при индексации
- Не оптимизирован для частых повторных запусков

## 🔌 Как включить плагины

### Архитектура плагинов
IntelliRefactor использует систему хуков для расширения функциональности:

```
c:\Intel\recon\intellirefactor\
├── plugins\
│   ├── __init__.py
│   ├── plugin_manager.py      # Менеджер плагинов
│   ├── hook_system.py         # Система хуков
│   └── examples/              # Примеры плагинов
```

### Создание собственного плагина
```python
# my_plugin.py
from intellirefactor.plugins.plugin_interface import BasePlugin

class MyAnalysisPlugin(BasePlugin):
    """Плагин для дополнительного анализа"""
    
    def __init__(self):
        super().__init__("my_analysis_plugin", "1.0.0")
    
    def on_analysis_complete(self, analysis_result):
        """Вызывается после завершения анализа"""
        # Добавляем собственные метрики
        analysis_result['custom_metrics'] = self.calculate_custom_metrics()
        return analysis_result
    
    def calculate_custom_metrics(self):
        return {"my_metric": 42}
```

### Регистрация плагина
```python
# В конфигурационном файле или через CLI
from intellirefactor.plugins.plugin_manager import PluginManager

plugin_manager = PluginManager()
plugin_manager.register_plugin(MyAnalysisPlugin())
plugin_manager.enable_plugin("my_analysis_plugin")
```

### Использование стандартных плагинов
```bash
# Список доступных плагинов
python -m intellirefactor plugins list

# Включение плагина
python -m intellirefactor plugins enable performance-analyzer

# Отключение плагина
python -m intellirefactor plugins disable legacy-code-detector
```

## 🛡️ Как безопасно запускать

### Dry-run режим (предварительный просмотр)
```bash
# Полный предварительный анализ
python -m intellirefactor analyze . --dry-run

# Предварительный просмотр рефакторинга
python -m intellirefactor refactor . --dry-run --verbose

# Предварительный просмотр применения конкретной возможности
python -m intellirefactor apply opportunity.json --dry-run --output preview.txt
```

### Система резервных копий
```bash
# Автоматическое создание бэкапа перед изменениями
python -m intellirefactor refactor . --create-backup

# Управление бэкапами
python -m intellirefactor safety list-backups
python -m intellirefactor safety restore-backup backup_20240108_120000

# Настройка директории бэкапов
python -m intellirefactor safety configure --backup-dir ./backups
```

### Уровни безопасности
```bash
# Консервативный уровень (только безопасные изменения)
python -m intellirefactor refactor . --safety-level conservative

# Умеренный уровень (баланс между агрессивностью и безопасностью)
python -m intellirefactor refactor . --safety-level moderate

# Агрессивный уровень (максимальные изменения)
python -m intellirefactor refactor . --safety-level aggressive --no-validation
```

### Валидация и проверка
```bash
# Запуск тестов после рефакторинга
python -m intellirefactor refactor . --validate-with-tests

# Проверка обратной совместимости
python -m intellirefactor refactor . --check-compatibility

# Генерация подробного отчета
python -m intellirefactor refactor . --generate-report detailed_report.json
```

### Аварийное восстановление
```bash
# Быстрый откат последних изменений
python -m intellirefactor safety rollback-last

# Проверка целостности проекта
python -m intellirefactor safety verify-integrity

# Восстановление из конкретного бэкапа
python -m intellirefactor safety restore --backup-id backup_12345
```

---
💡 **Советы по безопасности**:
- Всегда начинайте с `--dry-run`
- Используйте `--safety-level conservative` для первого рефакторинга
- Храните бэкапы вне директории проекта
- Запускайте тесты после каждого сеанса рефакторинга