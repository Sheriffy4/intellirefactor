# IntelliRefactor - Интеллектуальная система рефакторинга

## 🎯 Обзор

IntelliRefactor - это продвинутая система автоматического рефакторинга кода, разработанная специально для проекта Recon-DPI. Система обеспечивает безопасный, управляемый и интеллектуальный подход к рефакторингу сложных Python-проектов.

**Версия:** 2.0.0  
**Статус:** Production Ready  
**Лицензия:** MIT

---

## 🔧 Основные возможности

### ✅ Автоматический анализ кода
- **Глубокий статический анализ** с использованием AST
- **Обнаружение дубликатов кода** (block clone detection)
- **Поиск неиспользуемого кода**
- **Анализ архитектурных запахов**
- **Построение графов зависимостей**

### ✅ Интеллектуальный рефакторинг
- **Автоматическое разделение God Object'ов**
- **Рефакторинг на основе паттернов**
- **Сохранение обратной совместимости**
- **Постепенный рефакторинг (incremental refactoring)**
- **Валидация результатов**

### ✅ Безопасность операций
- **Автоматическое создание резервных копий**
- **Система отката изменений**
- **Валидация до/после рефакторинга**
- **Контроль разрушительных операций**

### ✅ Генерация документации
- **Автоматическая генерация архитектурных диаграмм**
- **Создание спецификаций (Requirements.md, Design.md)**
- **Flowchart'ы и call graph'ы**
- **Отчеты о рефакторинге**

---

## 📁 Структура проекта

```
intellirefactor/
├── __init__.py              # Главный API модуль
├── __main__.py              # Точка входа для python -m intellirefactor
├── main.py                  # Основная точка входа CLI
├── cli.py                   # Полная реализация CLI (~220KB)
├── api.py                   # Основной API класс IntelliRefactor (~32KB)
├── config.py                # Конфигурация системы (~18KB)
├── interfaces.py            # Интерфейсы и типы данных (~15KB)
│
├── analysis/                # Модули анализа кода (28 файлов)
│   ├── file_analyzer.py     # Анализ отдельных файлов
│   ├── project_analyzer.py  # Анализ всего проекта
│   ├── spec_generator.py    # Генерация спецификаций
│   ├── index_builder.py     # Построение индекса проекта
│   └── ...
│
├── documentation/           # Генерация документации (8 файлов)
│   ├── doc_generator.py     # Основной генератор
│   ├── architecture_generator.py
│   └── ...
│
├── orchestration/          # Оркестрация рефакторинга (4 файла)
│   ├── global_refactoring_orchestrator.py
│   └── ...
│
├── refactoring/            # Модули рефакторинга (6 файлов)
│   ├── auto_refactor.py
│   ├── cst_transformer.py
│   └── ...
│
├── safety/                 # Безопасность операций (6 файлов)
│   ├── backup_manager.py
│   ├── rollback_manager.py
│   └── ...
│
├── performance/            # Оптимизация производительности (6 файлов)
│   ├── database_optimizer.py
│   └── ...
│
├── plugins/                # Система плагинов (4 файла)
│   ├── plugin_manager.py
│   └── ...
│
└── visualization/          # Генерация диаграмм (1 файл)
    └── diagram_generator.py
```

---

## 🚀 Быстрый старт

### Установка

```bash
# Клонирование репозитория
git clone <repository-url>
cd recon/intellirefactor

# Установка зависимостей
pip install -r requirements.txt
```

### Основные команды CLI

```bash
# Анализ проекта
python -m intellirefactor analyze /path/to/project

# Генерация спецификаций
python -m intellirefactor generate spec /path/to/project

# Обнаружение дубликатов
python -m intellirefactor duplicates blocks /path/to/project

# Поиск неиспользуемого кода
python -m intellirefactor unused detect /path/to/project

# Комплексный аудит
python -m intellirefactor audit /path/to/project

# Выполнение рефакторинга
python -m intellirefactor refactor /path/to/file --dry-run
```

---

## 🛠️ Использование API

```python
from intellirefactor import IntelliRefactor, IntelliRefactorConfig

# Создание конфигурации
config = IntelliRefactorConfig(
    safety_level="high",
    backup_enabled=True,
    verbose=True
)

# Инициализация системы
refactor = IntelliRefactor(config)

# Анализ проекта
result = refactor.analyze_project("/path/to/project")

# Выполнение рефакторинга
refactor.refactor_file(
    "/path/to/file.py",
    strategy="god_object_split",
    dry_run=True
)
```

---

## 📊 Модули системы

### 1. Анализ (analysis/)
- **file_analyzer.py** - анализ отдельных файлов
- **project_analyzer.py** - анализ всего проекта
- **spec_generator.py** - генерация спецификаций
- **index_builder.py** - построение индекса

### 2. Документация (documentation/)
- **doc_generator.py** - основной генератор документации
- **architecture_generator.py** - архитектурные диаграммы
- **flowchart_generator.py** - диаграммы потоков

### 3. Рефакторинг (refactoring/)
- **auto_refactor.py** - автоматический рефакторинг
- **cst_transformer.py** - трансформации CST
- **intelligent_refactoring_system.py** - интеллектуальные стратегии

### 4. Безопасность (safety/)
- **backup_manager.py** - управление резервными копиями
- **rollback_manager.py** - система отката
- **safety_manager.py** - общая безопасность

---

## 🔍 Диагностика проблем

### Проблема: Requirements.md создается некорректно

**Решение:** Система теперь использует fallback-генерацию из реальных данных анализа.

**Проверка:**
```bash
python -m intellirefactor audit /path/to/project --emit-spec --verbose
```

### Проблема: Дубликаты в completed_analyses

**Решение:** Добавлена валидация и предотвращение дублирования записей.

---

## 📈 Производительность

### Оптимизации:
- Индексация проекта в SQLite базе данных
- Кэширование результатов анализа
- Оптимизированная работа с AST
- Параллельная обработка файлов

### Метрики:
- Анализ крупного проекта: ~30 секунд
- Генерация документации: ~10 секунд
- Рефакторинг файла: ~5 секунд (с валидацией)

---

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте feature branch (`git checkout -b feature/amazing-feature`)
3. Зафиксируйте изменения (`git commit -m 'Add amazing feature'`)
4. Запушьте в branch (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

---

## 📄 Лицензия

MIT License - см. файл LICENSE для подробностей.

---

## 🆘 Поддержка

- **Issues:** Создавайте issue в репозитории
- **Документация:** Смотрите docs/ директорию
- **Примеры:** См. examples/ директорию

**Разработано специально для проекта Recon-DPI**