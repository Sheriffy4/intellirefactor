# Архитектура модулей IntelliRefactor v2.0

## 📋 Обзор

Этот документ описывает современную архитектуру и модули проекта IntelliRefactor для диагностики проблем и понимания, где искать решения при возникновении ошибок.

**Версия:** 2.0.0  
**Дата обновления:** 2026-01-09  
**Цель:** Современная архитектура с учетом всех улучшений

---

## 🏗️ Структура проекта

```
intellirefactor/
├── __init__.py              # Главный API модуль
├── __main__.py              # Точка входа CLI
├── main.py                  # Основная точка входа
├── cli.py                   # CLI реализация (~218KB)
├── api.py                   # Основной API класс (~32KB)
├── config.py                # Конфигурация системы (~18KB)
├── interfaces.py            # Интерфейсы и типы данных (~15KB)
│
├── analysis/                # Модули анализа кода (28 файлов)
│   ├── file_analyzer.py     # Анализ отдельных файлов
│   ├── project_analyzer.py  # Анализ всего проекта
│   ├── spec_generator.py    # Генерация спецификаций
│   ├── index_builder.py     # Построение индекса
│   ├── architectural_smell_detector.py
│   ├── audit_engine.py
│   └── ...
│
├── documentation/           # Генерация документации (8 файлов)
│   ├── doc_generator.py     # Основной генератор
│   ├── architecture_generator.py
│   ├── flowchart_generator.py
│   └── ...
│
├── orchestration/           # Оркестрация рефакторинга (4 файла)
│   ├── global_refactoring_orchestrator.py
│   ├── refactoring_validator.py
│   ├── refactoring_reporter.py
│   └── ...
│
├── refactoring/             # Модули рефакторинга (6 файлов)
│   ├── auto_refactor.py
│   ├── cst_transformer.py
│   ├── intelligent_refactoring_system.py
│   └── ...
│
├── safety/                  # Безопасность операций (6 файлов)
│   ├── backup_manager.py
│   ├── rollback_manager.py
│   ├── safety_manager.py
│   └── ...
│
├── performance/             # Оптимизация (6 файлов)
│   ├── database_optimizer.py
│   ├── memory_manager.py
│   └── ...
│
├── plugins/                 # Система плагинов (4 файла)
│   ├── plugin_manager.py
│   ├── hook_system.py
│   └── ...
│
└── visualization/           # Генерация диаграмм (1 файл)
    └── diagram_generator.py
```

---

## 🔧 Основные точки входа

### 1. CLI (Command Line Interface)
**Файл:** `intellirefactor/cli.py` (~218KB)

**Основные команды:**
```bash
analyze          # Анализ проекта или файла
generate spec    # Генерация Requirements.md, Design.md, Implementation.md
duplicates       # Обнаружение дубликатов кода
unused           # Поиск неиспользуемого кода
audit            # Комплексный аудит проекта
smells           # Обнаружение архитектурных запахов
index            # Управление индексом проекта
refactor         # Выполнение рефакторинга
docs             # Генерация документации
```

### 2. API
**Файл:** `intellirefactor/api.py` (~32KB)

**Основной класс:** `IntelliRefactor`
```python
from intellirefactor import IntelliRefactor, IntelliRefactorConfig

config = IntelliRefactorConfig(safety_level="high")
refactor = IntelliRefactor(config)
result = refactor.analyze_project("/path/to/project")
```

### 3. Главный модуль
**Файл:** `intellirefactor/__init__.py`

**Экспортируемые компоненты:**
- `IntelliRefactor` - главный API класс
- `AnalysisResult`, `RefactoringResult` - типы результатов
- `IntelliRefactorConfig`, `SafetyLevel` - конфигурация

---

## 📦 Модули анализа (analysis/)

### Ключевые компоненты:

#### 1. **file_analyzer.py**
**Назначение:** Анализ отдельных файлов  
**Ключевые методы:**
- `analyze_file()` - полный анализ файла
- `extract_metrics()` - извлечение метрик
- `detect_code_smells()` - обнаружение запахов

#### 2. **project_analyzer.py**
**Назначение:** Анализ всего проекта  
**Использует:** `index_builder.py`, `file_analyzer.py`  
**Команда:** `intellirefactor analyze /path/to/project`

#### 3. **spec_generator.py**
**Назначение:** Генерация спецификаций  
**Ключевые методы:**
- `generate_requirements()` - Requirements.md
- `generate_design()` - Design.md
- `generate_implementation()` - Implementation.md

#### 4. **index_builder.py**
**Назначение:** Построение индекса проекта  
**База данных:** `.intellirefactor/index.db`  
**Команды:** `index build`, `index rebuild`, `index status`

#### 5. **audit_engine.py**
**Назначение:** Комплексный аудит проекта  
**Команда:** `intellirefactor audit /path/to/project`

---

## 📚 Модули документации (documentation/)

### Ключевые компоненты:

#### 1. **doc_generator.py**
**Назначение:** Основной генератор документации  
**Команда:** `intellirefactor docs generate`

#### 2. **architecture_generator.py**
**Назначение:** Генерация архитектурных диаграмм  
**Формат:** Mermaid

#### 3. **flowchart_generator.py**
**Назначение:** Генерация диаграмм потоков

---

## ⚙️ Модули оркестрации (orchestration/)

#### 1. **global_refactoring_orchestrator.py**
**Назначение:** Координация всех операций рефакторинга  
**Связан с:** `refactoring_validator.py`, `refactoring_reporter.py`

#### 2. **refactoring_validator.py**
**Назначение:** Валидация результатов рефакторинга

#### 3. **refactoring_reporter.py**
**Назначение:** Создание отчетов о рефакторинге

---

## 🔨 Модули рефакторинга (refactoring/)

#### 1. **auto_refactor.py**
**Назначение:** Автоматический рефакторинг  
**Стратегии:** god_object_split, extract_class, move_method

#### 2. **cst_transformer.py**
**Назначение:** Трансформации CST (Concrete Syntax Tree)

#### 3. **intelligent_refactoring_system.py**
**Назначение:** Интеллектуальные стратегии рефакторинга

---

## 🛡️ Модули безопасности (safety/)

#### 1. **backup_manager.py**
**Назначение:** Создание и управление резервными копиями

#### 2. **rollback_manager.py**
**Назначение:** Система отката изменений

#### 3. **safety_manager.py**
**Назначение:** Общая безопасность операций

---

## 🚀 Улучшения в версии 2.0

### ✅ Решенные проблемы:

1. **Requirements.md генерация**
   - Добавлен fallback механизм
   - Валидация реального содержимого
   - Использование snapshot данных

2. **Дубликаты в completed_analyses**
   - Добавлена уникальная валидация
   - Предотвращение повторных записей

3. **Организация документации**
   - Предотвращено перемещение в reports/
   - Улучшена структура docs/

4. **Mermaid валидация**
   - Проверка наличия узлов и связей
   - Автоматическое создание fallback диаграмм

---

## 🔍 Диагностика проблем

### Проблема: Requirements.md создается некорректно

#### ДИАГНОСТИКА:
**Корневая причина:** Использование команды `intellirefactor audit --emit-spec`, которая может не работать.

#### Цепочка выполнения:
1. `structured_ultimate_analyzer.py:1862` → `generate_file_requirements()`
2. `contextual_file_analyzer.py:714` → `generate_file_requirements()`
3. `intellirefactor/analysis/spec_generator.py:296` → `generate_requirements_from_audit()`

#### Места для проверки:
1. **`contextual_file_analyzer.py:714-800`** - КРИТИЧЕСКИЙ МОДУЛЬ
2. **`intellirefactor/analysis/spec_generator.py:296`**
3. **`intellirefactor/cli.py`** - команда `audit`

#### РЕШЕНИЕ:
Система теперь использует fallback-генерацию из реальных данных анализа.

---

## 📊 Производительность

### Оптимизации:
- Индексация в SQLite
- Кэширование результатов
- Параллельная обработка
- Оптимизированная работа с AST

### Метрики:
- Анализ проекта: ~30 секунд
- Генерация документации: ~10 секунд
- Рефакторинг файла: ~5 секунд

---

## 🛠️ Конфигурация

### Файлы конфигурации:
- `intellirefactor.json` - основная конфигурация
- `.intellirefactor/index.db` - база данных индекса

### Уровни безопасности:
- `low` - минимальная защита
- `medium` - стандартная защита
- `high` - максимальная защита (рекомендуется)

---

## 📈 Интеграция

### Точки интеграции:
1. **API уровень:** `intellirefactor.api.IntelliRefactor`
2. **CLI уровень:** команды `analyze`, `refactor`, `docs`
3. **Прямое использование:** импорт модулей

### Пример интеграции:
```python
from intellirefactor import IntelliRefactor

refactor = IntelliRefactor()
result = refactor.analyze_file("my_file.py")
print(result.metrics.complexity)
```

---

## 📚 Документация

### Генерируемые документы:
- `Requirements.md` - требования к системе
- `Design.md` - архитектурный дизайн
- `Implementation.md` - детали реализации
- Архитектурные диаграммы (Mermaid)
- Flowchart'ы и call graph'ы

### Команды генерации:
```bash
# Полная документация
intellirefactor docs generate /path/to/file.py

# Конкретный тип
intellirefactor docs type /path/to/file.py architecture
```

---

## 🆘 Поддержка и отладка

### Включение подробного логирования:
```bash
python -m intellirefactor analyze /path/to/project --verbose
```

### Диагностические команды:
```bash
# Проверка статуса системы
python -m intellirefactor system status

# Проверка совместимости
python -m intellirefactor system status --check-compatibility
```

---

**Документ обновлен:** 2026-01-09  
**Версия:** 2.0.0  
**Статус:** Актуальная документация